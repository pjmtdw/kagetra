- content_for :title do
  東大かるた会公式サイト 問い合わせフォーム結果

- content_for :js_start do
  ut_karuta_form

= haml :top_bar
%div
  - (1..@list.page_count).each do |page|
    - if page == @list.current_page
      %span= "[" + page.to_s + "]&nbsp;"
    - else
      %a(href="#{request.path_info}?page=#{page}")= "[" + page.to_s + "]&nbsp;"
%div
  ※ 返信済みフラグは景虎の全ユーザで共有されるのでメールで返信をした人は「済み」ボタンを押して下さい．
%table
  %thead
    %tr
      %th 返信済み
      %th 日時
      %th 名前
      %th メールアドレス
      %th 内容
  %tbody#list_form
    - @list.each do |item|
      %tr(style="vertical-align:top" data-item-id="#{item.id}")
        %td
          - if item.done
            %div.nowrap
              %span ○ 
              - if Time.now - item.updated_at < 7200
                %button.tiny.button-light.update_flag_cancel 取消
          - else
            %div.nowrap
              %span ×
              %button.tiny.update_flag_done 済み
        %td.item_date
          %span.nowrap= item.created_at.strftime("%Y-%m-%d")
          %span.nowrap= item.created_at.strftime("%H:%M:%S")
        %td.nowrap.item_name= item.name
        %td
          %a(href="mailto:#{item.mail}")= item.mail
        %td= item.body.to_s.gsub("\n","<br>")
