- use Zepto
- compress
  use Rack::Static,
    :urls => ["/images", "/js", "/css"],
    :root => "public"
  use Rack::Deflater
- nginx + unicorn
- show hint for shared password and user password
- authenticate with shared password for getting /user/list/:id
- authenticate with shared password for posting /usr/auth /user/auth_salt
- try / catch for each login javascript function
- add error callback for $.ajax and Backbone.Model.fetch
